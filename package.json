{
  "name": "todobackend",
  "version": "1.0.0",
  "description": "Super app",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf .next && cd ../ToDoApp && npm run build && cp -r .next ../toDoBackend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push origin main",
    "git:commit": "git add . && git commit -m buildFronEnd && git push origin main"
  },
  "author": "Marcos Congregado",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.8",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "mongoose": "^8.3.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
